#include <iostream>
#include <vector>
using namespace std;

void printSubsets(string s, int n, vector<string> temp)
{
  if (temp.size() == n)
    return;
}
  int main()
  {
    cout << "Hello, Subsets!" << endl;
    return 0;
  }